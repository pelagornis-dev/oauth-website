---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <div class="w-full bg-white rounded-2xl shadow-lg p-8 mt-8 border border-gray-100">
    <div class="flex flex-col items-center mb-6">
      <span class="text-3xl font-bold text-black mb-2">Pelagornis</span>
      <h1 class="text-xl font-bold text-black">비밀번호 재설정</h1>
      <p class="text-gray-500 text-sm mt-1">가입하신 이메일을 입력하세요</p>
    </div>
    <form
      class="space-y-4"
      method="POST"
      action="http://localhost:3000/api/v1/auth/forgot-password?locale=ko"
    >
      <input
        type="email"
        name="email"
        placeholder="이메일"
        required
        class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:ring-2 focus:ring-black focus:outline-none transition text-black bg-white"
      />
      <button
        type="submit"
        class="w-full py-3 rounded-lg bg-black text-white font-semibold hover:bg-gray-900 transition"
        >재설정 메일 보내기</button
      >
    </form>
    <div class="flex justify-between mt-6 text-sm">
      <a href="/login" class="text-black hover:underline">로그인</a>
      <a href="/register" class="text-gray-500 hover:underline">회원가입</a>
    </div>
  </div>
</Layout>
