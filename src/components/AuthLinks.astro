---
const { links = [] } = Astro.props;
---

<div class="flex justify-between mt-8 text-sm">
  {
    links.map((link) => (
      <a href={link.href} class={link.class || 'text-black hover:underline'}>
        {link.label}
      </a>
    ))
  }
</div>

